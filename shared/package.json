{
  "name": "@remote-stylus/shared",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "watch": "tsc --watch --preserveWatchOutput",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.2",
    "@types/simple-peer": "^9.11.8"
  },
  "dependencies": {
    "eventemitter3": "^5.0.1",
    "msgpackr": "^1.10.1",
    "simple-peer": "^9.11.1"
  }
}
